project_name: ScyCommerce

auth: keycloak
default_framework: aspnet
default_database: postgresql

services:
  Product:
    models:
      Product:
        price: decimal
        description: string
      Supplier:
        name: string